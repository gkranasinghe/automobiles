import eventActions from '../../features/event/eventActions';
import queryActions from '../../features/home/queryActions';

const allActions = {
  eventActions,
  queryActions,
};

export default allActions;
